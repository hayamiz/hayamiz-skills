# Gardener Journal

## 2026-05-10

**Summary**: First gardener run on this repo. Docs and packaging conventions are healthy; the main gap was the absence of the Karpathy behavioral rules in `CLAUDE.md`. Fixed inline along with several small hygiene improvements.

**Sections run**: Documentation sync, Best practices audit, Skill extraction, Security hygiene, Repository hygiene, CLAUDE.md behavioral rules.

### Findings
- 1 critical, 6 recommended, 7 info

### Actions Taken
- Appended the four Karpathy rules under `## Coding Guidelines` in `CLAUDE.md`.
- Added a justification comment for `claudeCode.allowDangerouslySkipPermissions: true` in `.devcontainer/devcontainer.json`.
- Broadened `.gitignore` to cover `.env*`, `*.pem`, `*.key`, `credentials.json`, `secrets/`, `*.local.json` (with an exception for `.devcontainer/inside-container.settings.local.json`), and OS cruft.
- Removed the stale `hayamiz-skills` rename gotcha from `CLAUDE.md`.
- Initialized this journal.
- Added `.pre-commit-config.yaml` (gitleaks secret scanner) and a minimal GitHub Action that runs it on PRs.
